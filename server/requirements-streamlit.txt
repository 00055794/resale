# --- ReSALE dev / CI dependencies ---
# Everything in the runtime image plus the lint/type/test toolchain. Used by CI
# and local development:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
mypy==1.11.2
